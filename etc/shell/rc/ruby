# ~/etc/shell/rc/ruby
# working with ruby

CHRUBY_PATH="$(brew --prefix)/share/chruby"
if [ -d "${CHRUBY_PATH}" ]; then
	source "${CHRUBY_PATH}/chruby.sh"
	source "${CHRUBY_PATH}/auto.sh"
fi

alias bx='bundle exec'
